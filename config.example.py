# Copy this file to `config.py` and fill in your MySQL credentials.
# `config.py` is gitignored so passwords are not pushed to GitHub.

DB_USER = "root"
DB_PASSWORD = "YOUR_MYSQL_PASSWORD"
DB_HOST = "localhost"
DB_NAME = "mf_benchmarking"
